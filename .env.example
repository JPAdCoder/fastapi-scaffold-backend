# API Configuration
API_V1_STR=/api/v1

# Server Configuration
SERVER_HOST=localhost
SERVER_PORT=8000

# Database Configuration
DATABASE_URL=postgresql://postgres:your-password@localhost:5432/your-database
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-password
POSTGRES_DB=your-database

# Security
# Generate a new key using: openssl rand -hex 32
SECRET_KEY=replace-this-with-your-secure-secret-key
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings
# Add your frontend URLs here
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8080","http://127.0.0.1:3000","http://127.0.0.1:8080"]

# API Documentation Settings
OPENAPI_JSON_URL=/openapi.json
OPENAPI_DOC_URL=/docs
OPENAPI_REDOC_URL=/redoc
PROJECT_NAME=FastAPI Code Generator - Development

# MinIO Configuration
MINIO_HOST=localhost
MINIO_PORT=9000
MINIO_BUCKET=your-bucket-name

# Test Configuration
# These values are only used during testing
TEST_USER_PASSWORD=test-password-123
TEST_USER_NAME=test.user
TEST_ROLE_ID=test-role-id
TEST_DIVISION_ID=test-division-id

# Note: This is an example configuration file.
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit your actual .env file to version control
# 4. Use different values for development, testing, and production environments
# 5. Keep your production credentials secure and never share them
